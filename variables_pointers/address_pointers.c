#include <stdio.h>
/**
 * main - addresses of the pointers
 * Return: always 0 (success)
 */
int main(void)
{
	int *i;
	char *p;

	printf("the address of i is: %p\n", &i);
	printf("the address of p is: %p\n", &p);

	return (0);
}
